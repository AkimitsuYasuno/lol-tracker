# テーマカスタマイズガイド

このガイドでは、アプリのカラーテーマを簡単に変更する方法を説明します。

## 📋 目次

1. [基本的なテーマ変更](#基本的なテーマ変更)
2. [カラーシステムの理解](#カラーシステムの理解)
3. [プリセットテーマ](#プリセットテーマ)
4. [カスタムカラーの追加](#カスタムカラーの追加)

## 基本的なテーマ変更

すべてのテーマカラーは `app/globals.css` で管理されています。

### ファイルの場所
```
app/globals.css
```

### カラー変更の手順

1. `app/globals.css` を開く
2. `.dark, html` セクション（42行目付近）を見つける
3. 変更したいカラー変数の値を編集
4. 保存すると自動的に反映されます

## カラーシステムの理解

### HSL形式について

カラーは `H S% L%` の形式で定義されています：

- **H (Hue)**: 色相 (0-360)
  - 0 = 赤
  - 120 = 緑
  - 240 = 青
  - 360 = 赤に戻る

- **S (Saturation)**: 彩度 (0-100%)
  - 0% = グレー
  - 100% = 鮮やかな色

- **L (Lightness)**: 明度 (0-100%)
  - 0% = 黒
  - 50% = 標準
  - 100% = 白

### 主要なカラー変数

```css
.dark, html {
  --primary: 221.2 83.2% 53.3%;       /* メインカラー（青） */
  --destructive: 0 84.2% 60.2%;       /* エラー/敗北カラー（赤） */
  --background: 222 47% 11%;          /* 背景色 */
  --foreground: 210 40% 98%;          /* テキスト色 */
  --card: 222 47% 15%;                /* カード背景 */
  --border: 217.2 32.6% 25%;          /* ボーダー色 */
}
```

## プリセットテーマ

### 1. デフォルト（League of Legends ブルー）

現在のテーマ。既に設定済みです。

```css
--primary: 221.2 83.2% 53.3%;        /* LoL ブルー */
--destructive: 0 84.2% 60.2%;        /* LoL レッド */
```

### 2. グリーンテーマ

自然な緑色のテーマ。

```css
.dark, html {
  --primary: 142 76% 36%;             /* フォレストグリーン */
  --primary-foreground: 210 40% 98%;
  --destructive: 0 84.2% 60.2%;       /* レッド（そのまま） */
}
```

### 3. パープルテーマ

エレガントな紫色のテーマ。

```css
.dark, html {
  --primary: 271 91% 65%;             /* バイオレット */
  --primary-foreground: 210 40% 98%;
  --destructive: 0 84.2% 60.2%;
}
```

### 4. オレンジテーマ

暖かいオレンジ色のテーマ。

```css
.dark, html {
  --primary: 25 95% 53%;              /* オレンジ */
  --primary-foreground: 222 47% 11%;  /* 暗いテキスト */
  --destructive: 0 84.2% 60.2%;
}
```

### 5. シアンテーマ

クールなシアン色のテーマ。

```css
.dark, html {
  --primary: 189 94% 43%;             /* シアン */
  --primary-foreground: 210 40% 98%;
  --destructive: 348 83% 47%;         /* ピンクレッド */
}
```

## カスタムカラーの追加

### LoL専用カラーの使用

既に定義されているLoL専用カラー：

```css
--lol-blue: 221.2 83.2% 53.3%;      /* LoLブルー */
--lol-red: 0 84.2% 60.2%;           /* LoLレッド */
--lol-gold: 45 93% 47%;             /* LoLゴールド */
```

コンポーネントで使用する方法：

```tsx
<div className="text-[hsl(var(--lol-gold))]">
  ゴールド獲得！
</div>
```

### 新しいカスタムカラーの追加

1. `app/globals.css` の `.dark, html` セクションに追加：

```css
.dark, html {
  /* ... 既存のカラー ... */

  --custom-purple: 280 89% 65%;
  --custom-cyan: 189 94% 43%;
}
```

2. コンポーネントで使用：

```tsx
<div className="bg-[hsl(var(--custom-purple))]">
  カスタムカラー
</div>
```

## 実践例

### 例1: プライマリーカラーを緑に変更

**変更前:**
```css
--primary: 221.2 83.2% 53.3%;  /* 青 */
```

**変更後:**
```css
--primary: 142 76% 36%;        /* 緑 */
```

### 例2: 背景を明るくする

**変更前:**
```css
--background: 222 47% 11%;     /* 暗い */
```

**変更後:**
```css
--background: 222 47% 18%;     /* 少し明るい */
```

### 例3: ボーダーを目立たせる

**変更前:**
```css
--border: 217.2 32.6% 25%;     /* 控えめ */
```

**変更後:**
```css
--border: 221.2 83.2% 53.3%;   /* ブルーで目立つ */
```

## チャート・グラフのカラー

ゴールド推移グラフなどのチャート色も変更できます：

```css
--chart-1: 221.2 83.2% 53.3%;   /* ブルーチーム */
--chart-2: 0 84.2% 60.2%;       /* レッドチーム */
--chart-3: 45 93% 47%;          /* ゴールド */
--chart-4: 160 60% 45%;         /* その他 */
--chart-5: 280 65% 60%;         /* その他 */
```

## ダークモード vs ライトモード

デフォルトではダークモードが有効です。ライトモードに切り替えるには：

### globals.css を編集

**変更前:**
```css
html {
  @apply dark;
}
```

**変更後:**
```css
html {
  /* dark クラスを削除 */
}
```

ライトモードのカラーは `:root` セクションで定義されています（4行目付近）。

## トラブルシューティング

### カラーが反映されない

1. ブラウザをリロード (Ctrl + F5 / Cmd + Shift + R)
2. 開発サーバーを再起動
   ```bash
   npm run dev
   ```

### 色がおかしい

- HSL値が正しい範囲内か確認
  - H: 0-360
  - S: 0-100
  - L: 0-100

### 暗すぎる/明るすぎる

- `L (Lightness)` の値を調整
  - 暗くする: 値を下げる (例: 53% → 40%)
  - 明るくする: 値を上げる (例: 53% → 65%)

## 便利なカラー選択ツール

オンラインツールでHSL値を簡単に見つけられます：

- [HSL Color Picker](https://hslpicker.com/)
- [Coolors](https://coolors.co/)
- [Adobe Color](https://color.adobe.com/)

## まとめ

1. `app/globals.css` を開く
2. `.dark, html` セクションを見つける
3. カラー変数のHSL値を変更
4. 保存して確認

テーマカスタマイズを楽しんでください！ 🎨
